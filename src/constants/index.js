const rootId = -1;

const MAX_LEVEL = 2;

const EditorMode = {
  base: 'Mode',
  markdown: 'markdown',
  noamel: 'normal'
};

const SourceFrom = {
  base: 'SourceFrom',
  demo: 'demo',
  customer: 'customer'
};

export default {
  rootId, MAX_LEVEL, EditorMode, SourceFrom
};
